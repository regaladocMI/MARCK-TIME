<UserControl x:Class="MarcTime.UI.Vistas.ConfiguracionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer>
        <StackPanel>
            <TextBlock Text="Configuración" Foreground="White" FontSize="24" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBlock Text="{Binding MensajeEstado}" Foreground="#4CD964" FontSize="12" Margin="0,0,0,16"/>

            <TextBlock Text="Sonidos de notificación" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <ItemsControl ItemsSource="{Binding EventosSonido}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#151515" CornerRadius="6" Padding="12" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="160"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding NombreEvento}" Foreground="White" VerticalAlignment="Center"/>
                                <ComboBox Grid.Column="1" ItemsSource="{Binding SonidosDisponibles}" DisplayMemberPath="NombreArchivo"
                                          SelectedItem="{Binding SonidoSeleccionado}" Margin="0,0,8,0"/>
                                <Button Grid.Column="2" Content="Probar" Padding="8,4" Margin="0,0,8,0" Command="{Binding ProbarSonidoCommand}"/>
                                <CheckBox Grid.Column="3" Content="Activo" IsChecked="{Binding Activo}" Foreground="White" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Button Content="Guardar sonidos" HorizontalAlignment="Left" Padding="12,6" Margin="0,4,0,28" Command="{Binding GuardarSonidosCommand}"/>

            <TextBlock Text="Cursos / actividades" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBlock Text="Estos son los nombres que aparecen en los desplegables de Horario y Tareas." Foreground="#888888" FontSize="11" Margin="0,0,0,10"/>
            <ItemsControl ItemsSource="{Binding Cursos}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#151515" CornerRadius="6" Padding="10" Margin="0,0,0,6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" Padding="4" Margin="0,0,8,0"/>
                                <Button Grid.Column="1" Content="Guardar" Padding="8,4" Margin="0,0,4,0"
                                        Command="{Binding DataContext.GuardarCursoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding CursoId}"/>
                                <Button Grid.Column="2" Content="Eliminar" Padding="8,4"
                                        Command="{Binding DataContext.EliminarCursoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding CursoId}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
<UserControl x:Class="MarcTime.UI.Vistas.TareasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <Grid DockPanel.Dock="Top" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Tareas" Foreground="White" FontSize="24" FontWeight="Bold"/>
            <Button Grid.Column="1" Content="+ Nueva tarea" Padding="12,6" Command="{Binding AgregarTareaCommand}"/>
        </Grid>

        <ScrollViewer>
            <ItemsControl ItemsSource="{Binding Tareas}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#151515" CornerRadius="6" Padding="14" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <CheckBox Grid.Column="0" IsChecked="{Binding Completada}" VerticalAlignment="Center" Margin="0,0,10,0"/>

                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Titulo}" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                        <TextBlock Text="{Binding FechaEntregaTexto}" Foreground="#999999" FontSize="11"/>
                                        <TextBlock Text=" · " Foreground="#999999" FontSize="11"/>
                                        <TextBlock Text="{Binding PrioridadTexto}" Foreground="#999999" FontSize="11"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Grid.Column="2" Orientation="Horizontal">
                                    <Button Content="Editar" FontSize="10" Padding="8,4" Margin="0,0,4,0"
                                            Command="{Binding DataContext.EditarTareaCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding TareaId}"/>
                                    <Button Content="Eliminar" FontSize="10" Padding="8,4"
                                            Command="{Binding DataContext.EliminarTareaCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding TareaId}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>